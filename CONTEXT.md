# Personal Coding Agent

一个面向个人长期使用的本地软件工程代理；当前上下文用于统一其产品边界与核心领域语言。

## Language

**个人 Coding Agent**：
在 Windows 本地运行、通过 CLI/TUI 操作，并能在真实代码仓库中持续协助软件工程工作的单用户代理。
_Avoid_: 教学 Demo、概念产品、云端多用户 Agent

**受监督的自主执行**：
用户给出目标后，Agent 自主探索、规划、修改并验证；仅在存在实质歧义、高风险操作或额外权限需求时暂停请求用户介入。
_Avoid_: 逐条指挥、完全无人监管

**仓库级软件工程闭环**：
Agent 围绕一个真实代码仓库完成从理解目标和代码，到修改、执行验证、迭代修正及交付说明的完整工作过程。
_Avoid_: 单次代码生成、仅聊天问答、通用桌面自动化

**工具权限**：
在工具执行前，根据操作目标、影响范围和可恢复性决定自动允许或请求用户批准的统一安全边界。
_Avoid_: 提示词约束、工具内部零散检查、默认完全信任

**任务会话**：
绑定一个工作区和一个用户目标的持久化执行历史；它可以在进程退出后恢复，并保持完整审计记录。
_Avoid_: 临时聊天记录、模型上下文窗口

**只读仓库调查**：
Wesly 通过列目录、搜索文本和读取文件理解真实代码仓库，并以文件路径为依据回答用户问题的首个纵向能力。
_Avoid_: 静态提示词问答、代码修改、任意命令执行

**工作区漂移**：
任务恢复时，当前 Git 状态或 Agent 最近观察过的文件版本与会话记录不一致的状态。
_Avoid_: 任意工作区变化、仅 Git dirty 状态

**恢复协调**：
通过前置状态、后置条件、幂等键或外部查询，证明崩溃时未完成操作的实际结果，再决定补记完成、自动重试或请求人工处理。
_Avoid_: 无条件重试、默认操作失败

**模型上下文**：
某一模型请求实际包含的有界指令、消息、工具定义和工具结果；它是任务会话历史按特定策略产生的视图。
_Avoid_: 完整任务会话、跨任务记忆、整个代码仓库

**上下文策略**：
从完整会话与可用资料中选择、排序和限制本轮模型上下文的版本化规则。
_Avoid_: 模型适配器、会话存储

**项目指令快照**：
Session 创建时从用户全局和工作区 `WESLY.md` 固定保存的带来源、目录作用域与内容哈希的指令集合；该 Session 内不热更新。
_Avoid_: 普通工具数据、跨任务记忆、动态文件读取
