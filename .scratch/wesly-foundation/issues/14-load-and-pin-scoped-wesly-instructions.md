# 14 — 加载并固定作用域化 WESLY.md

**What to build:** 新任务启动时，Wesly 从明确允许的全局和工作区位置加载适用的 `WESLY.md`，按目录作用域和优先级组装指令快照，并在该任务运行期间保持不变。

**Blocked by:** 13 — 完成搜索、读取与文件证据闭环

**Status:** ready-for-agent

- [ ] 只识别约定的全局文件和工作区内精确命名的 `WESLY.md`。
- [ ] 指令按内置安全、当前用户、最近目录、祖先、根和全局顺序生效。
- [ ] 每个指令区块带来源、作用域和内容哈希，普通工具数据不能提升为指令。
- [ ] Session 内使用创建时快照；磁盘文件变化不热重载，新 Session 才读取新版本。
- [ ] 扫描拒绝工作区外链接，跳过约定目录，并校验 UTF-8、单文件和总大小限制。
- [ ] 任一指令超限时整体以 `instructions_limit` 失败，不部分注入或静默截断。
