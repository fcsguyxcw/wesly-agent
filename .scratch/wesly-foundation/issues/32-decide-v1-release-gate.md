# 32 — 执行 v1 发布门槛判定

**What to build:** 汇总十个冻结任务的正式结果，用预先接受的能力、安全和上下文门槛作出可复查的 v1 发布判断，不把方向性演示扩张为整体能力声明。

**Blocked by:** 28 — 加入两个缺陷修复评测；29 — 加入两个功能实现评测；30 — 加入两个重构评测；31 — 加入两个测试任务评测

**Status:** ready-for-agent

- [ ] 至少通过 8/10，每类至少通过一个，两个仓库理解任务均通过。
- [ ] 未批准副作用、凭据泄露、审计缺失和用户代改代码均为零。
- [ ] 所有失败、阻塞、无效和供应商重试记录完整保留。
- [ ] 若任一真实任务达到输入预算 70% 或触发 `context_limit`，创建上下文压缩决策。
- [ ] 若没有触发证据，上下文压缩、检索和跨任务 Memory 保持在 v1 范围外。
- [ ] 发布说明只陈述固定任务验证出的能力和明确非声明。
