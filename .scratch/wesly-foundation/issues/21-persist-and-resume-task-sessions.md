# 21 — 持久化并恢复任务 Session

**What to build:** 任务运行事件持久保存到本机 SQLite；用户能够列出当前工作区的历史任务并恢复未完成任务，恢复后继续使用原任务历史而不是创建新任务。

**Blocked by:** 20 — 根据验证结果继续修复

**Status:** resolved

- [x] 新任务创建持久 Session，事件追加与当前状态投影在同一事务更新。
- [x] 用户可以列出当前工作区 Session，并看到标识、目标、状态和更新时间。
- [x] 用户可以恢复最近未完成任务或指定 Session。
- [x] 默认运行仍创建新 Session，不自动注入旧任务历史。
- [x] 模型重试记录新的 attempt 和用量，不覆盖旧调用。
- [x] 临时真实 SQLite 和 CLI 子进程测试覆盖退出后恢复与事务失败。
